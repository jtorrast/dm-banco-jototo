<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Bancojototo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/azul_electrico_palido</item>
        <item name="android:statusBarColor">@color/cardview_dark_background</item>
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
    <style name="ThemeOverlay.AppCompat.navTheme" parent="ShapeAppearance.Material3.NavigationBarView.ActiveIndicator">
        <!-- Color of text and icon when SELECTED -->
        <item name="colorPrimary">@color/white</item>

        <!-- Background color when SELECTED -->
        <item name="colorControlHighlight">@color/azul_oscuro_medio</item>
    </style>
</resources>